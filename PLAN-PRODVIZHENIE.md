# План модернизации leff-dev.ru

На основе разбора видео про SEO/AI-поиск 2026 и анализа текущего сайта.

Идея: сайт сейчас — чистая визитка (Главная / Кейсы / Цены / Контакты). Чтобы попадать в выдачу и AI-ответы, нужны страницы под конкретные «горячие» запросы ниши + одна центральная коммерческая страница, которую они усиливают.

---

## Порядок действий

### 1. Шаг 0 — техника
**30.08.2026 — полный повторный аудит по настоящему PDF-чек-листу Rush Agency («Техничка сайта: 30 пунктов»).** Первая версия этого раздела (см. архив внизу файла) была написана без реальной проверки и почти полностью не подтвердилась. В этот раз каждый пункт прогнан по-настоящему: `curl` по живому сайту, git-история коммитов, реальный мобильный Lighthouse-аудит (через Playwright/Chromium) и скриншоты 4 страниц на экране 360px. Полная таблица — в разделе «Аудит по чек-листу Rush Agency (30 пунктов)» ниже.

**Итог: фундамент сайта в целом здоровый, нашлось 2 настоящих бага — оба исправлены и перепроверены 30.08.2026:**
1. ✅ **LCP на странице «Кейсы» — было 3.8с при норме 2.5с.** Причина: первая (самая верхняя) картинка кейса грузилась с `loading="lazy"` вместо приоритетной загрузки — пункт 18 чек-листа («не вешать lazy на картинку первого экрана»). Файл: `src/components/CaseCard.tsx`, теперь первая карточка получает `preload`/`fetchPriority="high"`/`loading="eager"` (страница `/cases`). Попутно выяснилось через context7: в Next.js 16 старый проп `priority` у `next/image` — deprecated, актуальный API — `preload` (+ `fetchPriority`/`loading` отдельно, если нужен полный эффект) — код обновлён под него, а не под устаревший проп. **Перепроверено локальным Lighthouse-прогоном: LCP 3.8с → 2.3с, `lcp-discovery-insight` 0 → 1.**
2. ✅ **Недостаточный контраст кнопки «Обсудить проект»** (Главная, «Цены», «Смотреть сайт» на кейсах, номера этапов оплаты) — было: белый текст на `#3b82f6`, контраст 3.67:1 при норме 4.5:1. Добавлена отдельная переменная `--primary-strong: #2563eb` специально для кнопок с заливкой (обычный `--primary` не трогали — он на тёмном фоне используется для текста/иконок, и его контраст там уже был в порядке, менять его целиком было бы шагом назад). **Перепроверено: `color-contrast` 0 → 1.**

Сборка (`npm run build`) проходит чисто после обеих правок.

Осталось не проверено программно (нужен твой доступ):
- **Google Search Console / Яндекс.Вебмастер** — сколько страниц реально в индексе и по каким запросам показывается сайт помимо брендового.
- ✅ **`www.leff-dev.ru`** — доведено до конца 30.08.2026. Пользователь добавил A-запись на `www` в Selectel (та же VM, `89.232.177.22`). После этого сервер стал явно отклонять TLS для `www` (`ssl_reject_handshake` в `00-default.conf` — намеренная защита от чужих доменов на общий IP, см. комментарий в конфиге про инцидент с `therikky.su`), потому что для `www` не было ни блока в nginx, ни сертификата. По решению «делаем как рекомендуют в видео» — донастроен полный 301-редирект `www → apex`, а не просто оставлена голая DNS-запись:
  - `deploy/nginx/conf.d/leff-dev.conf` и `deploy/nginx/leff-dev-https.conf.example` в репозитории uaartist обновлены: порт 80 слушает оба имени (закрывает ACME-challenge для www и сразу редиректит голый http), плюс новый блок `:443 www.leff-dev.ru` с явным `return 301 https://leff-dev.ru$request_uri` — **не proxy_pass**, чтобы не получить дубль контента с кодом 200 на двух адресах.
  - Осталось выполнить на сервере (не сделано — нужен твой SSH-доступ):
    ```
    ssh leff@89.232.177.22
    cd ~/uaartist && git pull
    cp deploy/nginx/leff-dev-https.conf.example deploy/nginx/conf.d/leff-dev.conf
    sudo docker exec uaartist-nginx-1 nginx -s reload

    sudo docker run --rm -v uaartist_letsencrypt:/etc/letsencrypt \
      -v uaartist_certbot_www:/var/www/certbot certbot/certbot certonly \
      --webroot -w /var/www/certbot --cert-name leff-dev.ru --expand \
      -d leff-dev.ru -d www.leff-dev.ru

    sudo docker exec uaartist-nginx-1 nginx -s reload
    ```
    ✅ Выполнено 30.08.2026 — закоммичено, запушено, задеплоено, проверено вживую.

**Наблюдение (не железный факт, единичный запрос):** «Обзор от ИИ» в поиске по «leff dev» не связал сайт с запросом — перечислил разработчиков с похожей фамилией (Leff Raytleff, Adam Leff, Obie Leff), хотя органика сайт нашла верно. Может быть шумом, но перекликается с пунктом «сигналы доверия» ниже.

### 2. ✅ Усилить страницу «Цены» — выполнено 30.08.2026
Диапазоны цен, сроки и этапы оплаты там уже были готовы на момент проверки. Добавлено: убрана лишняя вводная фраза, блок FAQ (4 вопроса, только из уже подтверждённых фактов сайта), дата актуальности цен (`PRICES_UPDATED_AT` — константа в коде, не `new Date()`). Закоммичено, запушено, задеплоено.

### 3. ✅ Кейс uaartist (детально) — выполнено 30.08.2026
Отдельная страница `/cases/uaartist`: реальный таймлайн из git-истории и roadmap.md (даты, этапы, технические решения), честный текущий статус (каталог пуст, оплата в тестовом режиме) без выдуманных цифр трафика/продаж. Ссылка добавлена с карточки на `/cases`, страница — в sitemap. Закоммичено, запушено, задеплоено, проверено вживую.

### 4. ✅ «Fullstack-разработчик на Django + Next.js» (страница о себе) — выполнено 30.08.2026
Страница `/about`, добавлена в навигацию. Честное позиционирование: ~1 год непосредственно в разработке после нескольких лет системным администратором и DevOps в команде (микросервисная инфраструктура, отказоустойчивость) — без раздутого опыта и без выдуманных кейсов. Закоммичено, запушено, задеплоено, проверено вживую.

### 5. ✅ «Сколько стоит разработка сайта в 2026» — выполнено 30.08.2026
Реализовано шире изначальной формулировки (была «...на Next.js», стала про разработку сайта в целом) — с таблицей цен и сроков по типам проектов (данные вынесены в общий `services.ts`, используются и на «Цены», и в статье — не могут разойтись) и честным разбором на примере реального таймлайна uaartist, включая объяснение, почему свой проект за 4 дня ≠ клиентский срок 18 дней. Заодно построена блог-инфраструктура: `/blog` с карточками, `posts.ts`, хлебные крошки (компонент + BreadcrumbList JSON-LD, добавлены заодно и на `/cases/uaartist`) — обосновано тем, что публикации планируются регулярно (запуск продаж uaartist, итоги проекта и т.д.), не разовая акция. `/blog` добавлен в навигацию. Закоммичено, запушено, задеплоено, проверено вживую.

### 6. ✅ «Заказать интернет-магазин на Django под ключ» — выполнено 30.08.2026
Страница `/internet-magazin-na-django` — прямой ответ в первом абзаце (что входит, срок, цена — данные из общего `services.ts`, не задублированы), ссылка на кейс uaartist. Связана с сайтом: CTA на `/cases/uaartist`, ссылка из статьи-сравнения и с карточки на «Цены» (новое поле `detailHref` у Service). Закоммичено, запушено, задеплоено, проверено вживую.

### 7. ✅ «Конструктор сайта или разработка с нуля — что выбрать» — выполнено 30.08.2026
Переформулировано из изначально агрессивного черновика названия («Почему стоит выбрать... а не конструктор») в нейтральное сравнение — исходный вариант был бы и нечестным (конструкторы реально хороший выбор для многих задач), и звучал как наезд на Tilda/WordPress/Wix. Реальный пример — анонимизированный аудит WordPress-сайта (без названия сайта: раскрывать чужие уязвимости публично неэтично), с точной атрибуцией причины (конфликтующие плагины-конструкторы страниц + заброшенные обновления, не WordPress как таковой). Ссылка на кейс uaartist. Закоммичено, запушено, задеплоено, проверено вживую.

### 8. ✅ Микроразметка — выполнено 30.08.2026
Большая часть закрылась по ходу предыдущих шагов (Person на каждой странице, BreadcrumbList на кейсе и статьях блога, Article на обеих статьях). Отдельно добавлено: FAQPage на «Цены» (контент уже был написан на шаге 2, просто размечен) и Service/Offer — 7 блоков на «Цены» + 1 на странице интернет-магазина на Django, с числовым `minPrice` через новый хелпер `parsePriceValue`. Product и Organization осознанно не нужны (сайт не продаёт товары, представляет физлицо, не компанию). Закоммичено, запушено, задеплоено, проверено вживую.

**Осталось на твоей стороне** (не могу проверить сам): прогнать `https://leff-dev.ru/price` и `/internet-magazin-na-django` через [Google Rich Results Test](https://search.google.com/test/rich-results) — валидатор укажет, если где-то в разметке есть формальная ошибка, которую не видно по одному только выводу JSON.

**Весь план из 8 шагов выполнен.**

---

## Сигналы доверия, которых пока не хватает

- Реальное имя автора у кейсов + дата публикации/обновления
- Мини-страница «обо мне»: опыт, почему можно доверять (пересекается с п.4)
- Цифры и результат в кейсах, а не только список технологий

---

## Аудит по чек-листу Rush Agency (30 пунктов) — 30.08.2026

Источник — PDF «Техничка сайта: 30 пунктов, которые чинятся первыми» (приложение к видео Rush Agency). Каждый пункт проверен реально: `curl` на живом сайте, git-история, мобильный Lighthouse-аудит (Playwright/Chromium, throttling под 4G) на `/` и `/cases`, скриншоты 360px на всех 4 страницах.

**Индексация и доступность (1–7)**
1. Склейка зеркал — ✅ `http→https` на `leff-dev.ru` работает (301, единый адрес). Записи `www` в DNS нет вообще (подтверждено в панели провайдера) — значит и дублирующего адреса, который надо было бы склеивать, не существует. Пункт закрыт.
2. robots.txt не блокирует JS/CSS — ✅ `Allow: /` для всех, ничего не закрыто.
3. robots.txt закрывает только служебное — н/п: на сайте нет корзины/кабинета/поиска, закрывать нечего.
4. Sitemap.xml живой и совпадает с реальностью — ✅ отдаёт все 4 реальных URL с кодом 200. Нюанс: `lastmod` генерируется как `new Date()` в момент сборки/запроса, а не привязан к реальной дате правки контента — не ошибка чек-листа, но и не даёт настоящего сигнала свежести.
5. Коды ответа сервера — ✅ несуществующая страница отдаёт честный 404.
6. Canonical проставлен и указывает сам на себя — ✅ на всех 4 страницах, включая варианты с `?utm_source=...` (canonical корректно ведёт на чистый адрес).
7. Нет случайного noindex — ✅ ни meta robots, ни заголовка `X-Robots-Tag` нет ни на одной странице.

**Дубли и мета-теги (8–13)**
8. Уникальный title на каждой странице — ✅ все 4 разные.
9. Уникальный description — ✅ все 4 разные.
10. Один H1, не дублирует title дословно — ✅ проверено рендером всех 4 страниц, H1 везде короче и не совпадает с title.
11. GET-параметры не плодят копии — ✅ canonical чистит `utm`-метки.
12. Один адрес — один слеш — ✅ `/price/` 301-редиректит на `/price`.
13. Пагинация не создаёт дублей — н/п: пагинации на сайте нет.

**Скорость и Core Web Vitals (14–19)** — реальный мобильный Lighthouse, throttling под 4G:
14. LCP до 2,5с — Главная: 2.3с ✅. Кейсы: было 3.8с ❌, **исправлено → 2.3с ✅** (см. «Шаг 0», баг №1).
15. CLS до 0,1 — ✅ 0 на обеих проверенных страницах.
16. INP до 200мс — не измеряется в лабораторных условиях без реального взаимодействия; прокси-метрика Total Blocking Time: 300–500мс на разных страницах — не провал, но не идеал, стоит присмотреться при следующей оптимизации.
17. Картинки в современном формате и нужного размера — Next.js `<Image>` сам отдаёт WebP/AVIF через встроенный оптимизатор. Lighthouse нашёл переразмер у 2 картинок на «Кейсах» (~25 КБ лишних) — мелочь, не критично.
18. Ленивая загрузка не у главной картинки экрана — было ❌, **исправлено ✅**, см. «Шаг 0», баг №1.
19. Кэширование и сжатие — ✅ `gzip` включён, `Cache-Control: s-maxage=31536000` на статике.

**Мобильная версия (20–22)** — проверено скриншотами 360px (Playwright) на всех 4 страницах:
20. Контент на мобильной версии тот же, что на десктопе — ✅ ничего не спрятано.
21. Ничего не наезжает и не режется — ✅ `scrollWidth === clientWidth` на всех 4 страницах, визуально тоже чисто.
22. Кнопки от 44×44px — Lighthouse не подсветил проблем с tap-target'ами; визуально кнопки достаточного размера. Точечных замеров каждой не делал.

**Структура и перелинковка (23–26)**
23. Хлебные крошки на внутренних страницах — н/п: сайт плоский (4 страницы на одном уровне), хлебным крошкам показывать нечего.
24. Нет страниц-сирот — ✅ все 4 страницы есть и в шапке, и в футере.
25. До важной страницы — 3 клика с главной — ✅ тривиально выполняется, всё в 1 клике.
26. Внутренние ссылки не через редиректы — ✅ хедер/футер ссылаются на канонические адреса напрямую.

**Микроразметка (27–30)**
27. Organization на всём сайте — реализовано как `Person` (schema.org), не `Organization` — **это осознанно верный выбор**: сайт представляет физлицо-фрилансера, а не компанию, `Person` — правильный тип разметки для этого случая, а не недоработка.
28. Product на карточках товара — н/п: сайт не продаёт товары.
29. Article/FAQPage в блоге — н/п: блога пока нет (см. план контента выше).
30. BreadcrumbList — н/п: хлебных крошек нет, см. п.23.

**Итог:** из 30 пунктов реально применимо ~23, все 23 в порядке (2 найденных бага — LCP на «Кейсах» из-за lazy-загрузки и контраст CTA-кнопки, последний не из чек-листа, а из отдельного accessibility-аудита — исправлены и перепроверены 30.08.2026), 7 неприменимо для сайта такого масштаба (нет каталога/блога/пагинации/кабинета).

<details>
<summary>Архив: первая (недостоверная) версия этого раздела от 29.08.2026</summary>

Ниже — то, что было написано в плане до реальной проверки. Почти всё не подтвердилось при аудите 30.08.2026 выше — оставлено только как напоминание не доверять несверенным находкам.

- ~~Сайт не найден в поиске~~ — опровергнуто. 1-е место по брендовому запросу.
- ~~Дубль meta description~~ — опровергнуто. Все 4 страницы уникальны, исправлено ещё до этого плана (24.08.2026).
- Title и H1 в порядке — подтвердилось.
- ~~Микроразметки не видно~~ — опровергнуто, JSON-LD подключена.
- Блога, хлебных крошек и FAQ нет — подтвердилось.

</details>

---

## Идеи из кейса Bitex Sibir (второе видео Rush Agency)

Кейс: агентство пересобрало сайт завода лакокрасочных материалов с Bitrix на новый сайт с помощью Claude Code — трафик вырос в 10 раз за счёт SEO-архитектуры, а не просто факта использования AI.

- **Кейсы стоит описывать по этой же логике: с цифрами и результатом** (рост трафика с 280 до 3000 визитов, конкретные сроки) — не список технологий.
- **6 составляющих «идеального для SEO» сайта** (актуально, если в будущем будешь делать клиентам не лендинги, а многостраничные проекты с каталогом): каталог, карточки товаров, служебные страницы, блог, техническая часть, собственные инструменты.
- **Структура каталога должна повторять реальные запросы**, а не общие категории — не «краска», а «фасадная краска по бетону для наружных работ». Аргумент для клиентов по интернет-магазинам (uaartist и будущие проекты).
- **Чек-лист безопасного переезда сайта** (7 шагов, может пригодиться как отдельная услуга/пункт в портфолио): полная копия старого сайта, проектирование структуры до вёрстки, карта соответствия адресов (редиректы на смысловой аналог, не на главную), адаптация контента под новые шаблоны, техдолги закрываются сразу, мониторинг индексации (не трафика) в первые дни, не переезжать в сезон клиента.
- **AI не заменяет SEO-планирование.** В кейсе даже готовый AI-сайт при аудите agency нашли 56 недоработок — не из-за качества кода, а из-за того, что структуру и семантику нужно прорабатывать до кодинга. Тезис для позиционирования себя как разработчика, который делает не просто «красиво», а с пониманием SEO-основы.

---

## Шаг 9 (новый, из третьего видео про 13 бесплатных каналов трафика) — план на 31.08/01.09.2026

### Идея калькулятора стоимости сайта

Из видео: бесплатный виджет/инструмент на сайте, которым люди пользуются и делятся — сам генерирует трафик и возвраты без рекламы (пример из видео: калькулятор скорости интернета у конкурента клиента агентства тянул тысячи визитов в месяц). Подходит именно этому сайту, потому что использует навык пользователя (кодить), а не то, чего у него нет (видео, монтаж, Reels — их тоже разбирали в видео, но отклонили как не подходящие).

**Решили: считать по блокам функциональности, не по этапам разработки.** Этапы (ТЗ, бэкенд, фронтенд, SEO-политика) — это внутренний процесс, клиент не выбирает их галочкой, для калькулятора не годится. Блоки функциональности (страницы, каталог, корзина, личный кабинет...) — это ровно то, из чего клиент реально выбирает.

### Структура (согласовано с пользователем)
- Отдельная страница `/calculator` (не виджет внутри «Цены») — самостоятельный шарибельный инструмент
- Количество страниц — степпер/число, не чекбокс
- Остальное — чекбоксы
- Итоговая цена с пометкой «ориентировочно, точную посчитаю под задачу» — не жёсткое обязательство
- **Кнопка «Не нашли то, что нужно?» → `/contacts`** для нестандартных запросов, которые калькулятор не покрывает

### Список блоков — с честной пометкой источника цифры

Все цифры ниже, помеченные «реальная», взяты из уже опубликованной `services.ts` — калькулятор физически не может разойтись с «Цены», раз считает из тех же чисел.

**Структурное:**
- Количество страниц: 1 страница — база (0 ₽ сверху), дальше **+3000 ₽/страница** (реальная, выведена из разницы «лендинг 18 000 ₽» → «визитка до 5 страниц 30 000 ₽»: (30000-18000)/4 = 3000/стр.)

**Интернет-магазин (пакетом, не по отдельности):**
- «Каталог + корзина + оплата + доставка» — **+37 000 ₽** (реальная, разница «лендинг» → «магазин под ключ» 55000-18000). **Решение: не дробить на 4 отдельных чекбокса** — в текущем прайсе эти 4 пункта никогда не оценивались по отдельности, дробление означало бы выдумывать цифры, которых нет. Один чекбокс на весь пакет — честно и без гадания.

**Аккаунты:**
- Личный кабинет с бонусной/скидочной системой — **+18 000 ₽** (реальная, есть отдельной строкой в прайсе)

**Контент:**
- Блог с админкой для публикаций — **цифры нет, нужна оценка пользователя** (в текущем прайсе блог отдельно никогда не продавался — обсудить, сколько это должно стоить, прежде чем зашивать в код)

**Нестандартное (без числа, сразу на CTA):**
- Кастомный конструктор/калькулятор товара (как в uaartist), нетиповые интеграции — не считать калькулятором вообще, сразу вести на кнопку «Обсудить» → `/contacts`, потому что оценивается индивидуально

### Открытый вопрос к пользователю (до кодинга)
Сколько стоит блог как отдельная опция — единственная цифра, которой сейчас физически нет ни в прайсе, ни в этом плане. Без ответа этот один чекбокс либо не показываем, либо угадываем — лучше спросить.

### Техническая реализация (набросок, не начато)
- `src/data/calculator.ts` — базовая цена, приращения, аналогично `services.ts`
- `src/app/calculator/page.tsx` — client component (нужен стейт для чекбоксов/степпера и живого пересчёта суммы)
- Переиспользовать стиль карточек сайта (`bg-surface`, `border-surface-border`, `bg-primary-strong` для кнопок)
- Добавить в sitemap, связать ссылкой с «Цены» (и, возможно, с новой страницой про интернет-магазин на Django)
- Продолжить разработку начиная с уточнения цены блога, затем макет страницы, затем код

---

## Шаг 10 — независимый SEO-аудит (31.08.2026, `problems.md`) — доработка на завтра

Пользователь заказал независимый аудит сайта в отдельной сессии («как специалист, не знающий, что делали сегодня») — полезная перепроверка со стороны. M1 и M2 ниже уже исправлены и задеплоены в этой же сессии; остальное — на потом, приоритет по убыванию отдачи из самого отчёта.

**✅ Исправлено сразу (31.08.2026):**
- **M1** — Twitter-теги (`twitter:title`/`description`) были захардкожены под главную на всех 9 страницах (та же ошибка наследования, что раньше нашли и починили для `openGraph`, только в соседнем поле). Добавлено на каждую страницу.
- **M2** — на `/about`, `/cases`, `/price`, `/contacts`, `/blog` вообще не было `og:image` (не общая картинка, а пусто — file-конвенция `opengraph-image.tsx` не наследуется на дочерние роуты, как я ошибочно полагал). Добавлены персональные превью на все 5.

**❌ Не подтвердилось при перепроверке — H3 (индексация):**
Аудит написал «нет файлов верификации, сайт не в индексе (`site:` — 0 результатов)». Проверено: файлы верификации Google/Yandex реально существуют и отдают 200 (аудит физически не мог их найти внешним анализом — хэш-имена не угадать). `site:`-проверка — известно ненадёжный метод именно для этого домена (тот же WebSearch-инструмент показывал 0 результатов ещё утром, когда пользователь лично в приватном окне видел сайт на 1-м месте по «leff dev» живым скриншотом). Ложная тревога инструмента, не сайта — трогать не нужно.

**На доработку, по убыванию отдачи (из отчёта):**

1. **H1 — тонкий контент.** Страницы короткие: `/` 136 слов, `/cases` 116, `/blog` 92, `/about` 240, `/internet-magazin-na-django` 207, `/price` 364, обе статьи блога 385/414, `/cases/uaartist` 606 (лучшая по объёму). Для конкурентных коммерческих запросов обычно нужно 900+ слов на money-страницах, 1500+ на гайдах. **Важно: не лить воду ради счётчика слов** — добавлять только реальную глубину (разбор возражений, детали процесса, примеры), по образцу уже написанного `/cases/uaartist`.
2. **H2 — title/H1 главной не бьёт по коммерческому запросу.** Сейчас «Fullstack-разработчик» без «разработка сайтов и интернет-магазинов». Требует обсуждения формулировки с пользователем — это ещё и позиционирование, не только техника.
3. ✅ **H4 — три страницы конкурируют за интент «стоимость». Сделано 31.08.2026.** Подтвердилось, что дробление на 3 страницы — верное (так и советовали в видео: отдельная страница под каждый горячий запрос), а претензия аудита была не «удалить», а «развести по интенту + перелинковать». Каждой странице задана одна явная роль:
   - `/price` — транзакционный хаб, оставлен коротким. Уточнён подзаголовок («вилки по всем типам проектов»), добавлен блок «Разобраться подробнее» с исходящими ссылками на гайд и на страницу Django-магазина.
   - `/internet-magazin-na-django` — сервисная страница под нишевый запрос (квалификатор «на Django» уже отделяет её от `/price`). Добавлена ссылка на гайд, нижние ссылки собраны в колонку.
   - `/blog/skolko-stoit-razrabotka-sayta` — информационный гайд, расширен с ~385 до ~900+ слов **реальной глубиной** (7 факторов цены, привязанных к прайсу и кейсу uaartist; 6 этапов срока; 4 частые ошибки бюджета; практический пример на uaartist). Featured-snippet-абзац «Короткий ответ» в начале, ссылки вверх на `/price` и `/internet-magazin-na-django`, блок «Ещё по теме» (Django-страница, статья про конструктор, кейс uaartist). `dateModified` → 2026-08-31, помечено «обновлено».

   `posts.ts` — excerpt гайда обновлён под новое содержание. Сборка и TypeScript проходят, все 3 страницы отдают 200, оверфлоу при 360px нет (проверено рендером desktop + 360px). **Не закоммичено.** Возможна переделка после калькулятора (Шаг 9) — по решению пользователя.

   **Найдено попутно (не из этой задачи, предсуществующий баг):** футерная навигация (`flex items-center gap-6`, 6 ссылок без переноса) вылезает на 12px при ширине 360px → горизонтальный скролл на всех страницах. Пункт 21 аудита из Шага 0 («scrollWidth === clientWidth на всех 4 страницах») теперь устарел — навигация выросла, когда 30.08 добавили «Обо мне» и «Блог». Фикс ~1 строка (`flex-wrap` на футерной nav; хедерная nav `hidden sm:flex`, на мобильном её нет). Не чинил — вне scope H4, ждёт решения.
4. **M3 — `/internet-magazin-na-django` полу-осиротевшая.** Нет в основной навигации, всего 3 ссылки на неё в теле сайта. Решить: добавить в меню саму страницу, или завести хаб «Услуги».
5. **M4 — слабая внутренняя перелинковка.** Хлебные крошки есть в JSON-LD, но не видны в UI на некоторых страницах (проверить/добавить видимый компонент `Breadcrumbs` там, где ещё не стоит); статьи блога не ссылаются друг на друга — добавить блок «похожие статьи».
6. **M5 — 4 из 5 кейсов без индексируемого разбора.** Только у uaartist есть страница-кейс; Propheters и demo-лендинги (Просвет, Пур Пур ×2) — просто карточка + внешняя ссылка на `*.vercel.app`. По структуре `/cases/uaartist`, честно помечая демо как демо, можно сделать короче (400–600 слов).
7. **Хозчасть (низкий приоритет):** `apple-touch-icon` + `manifest.json` (сейчас 404), AVIF в `images.formats` (сейчас только WebP), RSS/Atom для блога, `og:site_name`/`og:locale` глобально (не только на главной), `Article`-схема без `image`/`publisher`, `Person` без `sameAs` на GitHub, шрифты (4 woff2 в preload — можно сократить subset).

**Не трогать / уже известно:**
- M6 (`lastmod` одинаковый у всех URL, генерируется `new Date()` на билде) — обсуждали ещё на шаге 0, осознанно оставлено.
- L7 (FAQPage больше не даёт rich-результат в Google) — то же самое выяснили сами сегодня, аудит независимо подтвердил.
